# Netlify deploy sozlamasi
[build]
  publish = "public"

# Statik sayt — build buyrug'i kerak emas
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
